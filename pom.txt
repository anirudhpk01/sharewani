I’ve gone through it carefully. Quick takeaway:

✅ Infra is good (Mongo pool, Redis pipelines, ECS, async)
❌ Data flow is inefficient (full re-reads, Redis-heavy, over-fetching)

We’ll reuse infra, replace the pipeline.

✅ Step 1 — Minimal CDC + Trim ONLY (no processing, no Redis)

Goal:

Prove we can ingest 15–20k events/sec and trim safely without lag

No workers yet. No batching yet. Just correctness + speed.

🔍 Where we hook in (important)

You already have:

CDC engine in MongoDBConnectionPool._watch_cdc_stream()
Queues: change_queue, etc.

👉 BUT current system:

pushes IDs
re-fetches full conversation ❌

We will bypass that logic

✂️ What we will build (new minimal pipeline)

Inside CDC consumer:

CDC event → extract 8 fields → log/metric → DONE

No queue yet.

⚠️ VERY IMPORTANT RULE

We will NOT modify existing CDC flow yet

Instead:

Add a parallel lightweight CDC consumer (shadow mode)

So:

existing system keeps running
we validate new pipeline safely
🧱 What you implement now
1️⃣ Add a new function (in connectionpool or new file)

Something like:

async def handle_cdc_event_minimal(change_event):
    """
    STEP 1 PIPELINE:
    - Extract only required fields
    - Drop everything else immediately
    - No processing, no Redis
    """

    full_doc = change_event.get("fullDocument")
    if not full_doc:
        return

    # 🔥 extract ONLY what you need
    minimal = {
        "conversationId": full_doc.get("conversationId"),
        "market": full_doc.get("market"),
        "createdDateTime": full_doc.get("createdDateTime"),
        # TODO: add your 5 more fields
    }

    # 🚨 CRITICAL: drop reference early
    change_event["fullDocument"] = None
    full_doc = None

    # TEMP: just count/log
    return minimal
2️⃣ Hook into CDC stream

Inside _watch_cdc_stream():

👉 Find where change events are received

Add:

minimal = await handle_cdc_event_minimal(change_event)

BUT:

don’t break existing flow
just run this alongside
3️⃣ Add ingestion metrics (VERY IMPORTANT)

Inside that function:

import time

LAST_LOG = time.time()
COUNT = 0

async def handle_cdc_event_minimal(change_event):
    global COUNT, LAST_LOG

    COUNT += 1

    if time.time() - LAST_LOG >= 1:
        print(f"[CDC INGEST] rate={COUNT}/sec")
        COUNT = 0
        LAST_LOG = time.time()
🎯 What we are testing now

After you run:

You should see:

[CDC INGEST] rate=15000/sec
❓ I need answers before Step 2
Where exactly is _watch_cdc_stream() defined in your file?
(just confirm file + function name)
Are you currently using fullDocument: "updateLookup" in your change stream?
What are the exact 8 fields you need? (list them)
🚫 Don’t do yet
no workers
no batching
no Redis
no queues

Once this is stable:

👉 Step 2 = introduce bounded queue + backpressure

Reply with:

answers to the 3 questions
and tell me when Step 1 is wired in

We’ll go tight and fast from there.
