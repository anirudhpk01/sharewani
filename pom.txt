You are analyzing an enterprise backend codebase.
Your task is to extract ONLY the architectural and runtime-relevant details needed to design a high-throughput streaming pipeline (Mongo CDC → processing → Redis).

DO NOT dump entire files. DO NOT give generic summaries.
Give structured, precise, implementation-level details.

---

1. Runtime Environment

- Language(s) used
- Framework (if any)
- Deployment environment (ECS/Fargate/EC2/etc.)
- CPU / memory configuration (if visible)
- Concurrency model:
  - single-threaded / multi-threaded / async workers / worker threads / processes
  - how parallelism is currently implemented

---

2. MongoDB Integration

- How MongoDB client is initialized
- Where connection logic lives (file/module structure)
- Whether connection pooling is configured
- Any existing Change Stream / CDC usage
- How queries are typically executed (sync/async, wrapper functions, repositories, etc.)
- Any middleware/abstraction over Mongo

---

3. Redis Integration

- How Redis client is initialized
- Library used (ioredis, redis, etc.)
- Whether pipelining or batching is already used anywhere
- Connection pooling / cluster / sentinel usage
- Existing patterns for:
  - SET / GET
  - Hash updates
  - Bulk operations

---

4. Secret Management (CRITICAL)

Explain EXACTLY how secrets are handled:

- Which secret manager is used (AWS Secrets Manager / Vault / env files / etc.)
- How secrets are fetched:
  - at startup?
  - per request?
  - cached?
- Where this logic exists (file/module names)
- How credentials are injected into:
  - Mongo connections
  - Redis connections
- Whether secrets are rotated dynamically
- Any wrapper/utility used for secrets

---

5. Application Structure

- Folder/module structure (high level, not every file)
- Where core services live (controllers, services, utils, etc.)
- How new modules/services are typically added
- Any event-driven or queue-based components already present

---

6. Logging + Observability

- Logging library used
- Whether structured logging exists
- Any metrics/tracing setup (Prometheus, CloudWatch, etc.)

---

7. Constraints / Rules (VERY IMPORTANT)

- Any enforced coding patterns (e.g., must use certain wrappers)
- Restrictions on:
  - creating new connections
  - using threads/workers
  - external libraries
- Any architectural constraints (layering, DI, service boundaries)

---

8. Example Flow (IMPORTANT)

Pick ONE real API or background process and describe:

- How request/data flows through the system
- Which layers it touches
- How DB/Redis calls are made

---

Output Format

- Use clear sections (as above)
- Be concise but precise
- Prefer bullet points over paragraphs
- Include small code snippets ONLY where necessary to explain patterns

---

Your goal: enable designing a high-throughput, low-latency streaming system that integrates cleanly into this codebase.